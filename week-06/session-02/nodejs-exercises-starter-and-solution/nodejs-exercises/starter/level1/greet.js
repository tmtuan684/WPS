// Exercise 1.3 — Greeting with process.argv
// Run with: node greet.js Tuan

// TODO: Log process.argv first and inspect its structure.
console.log(process.argv);

// TODO: Extract the name from process.argv (remember: index 0 is the
// path to node, index 1 is the path to this script, so real arguments
// start at index 2).
const name = undefined; // <-- replace this

// TODO: Print "Hello, <name>! Welcome to Node.js."
// If no name was provided, print a default greeting instead of "undefined".
