// Exercise 2.4 — File System Basics (use the *Sync fs methods)
const fs = require('fs');
const path = require('path');

// TODO 1: Create a folder called "notes" if it doesn't already exist.
//   Hint: fs.existsSync() and fs.mkdirSync()

// TODO 2: Write notes/todo.txt containing three to-do items, one per line.
//   Hint: fs.writeFileSync()

// TODO 3: Read the file back and print its contents.
//   Hint: fs.readFileSync()

// TODO 4: Append one more to-do item without overwriting existing content.
//   Hint: fs.appendFileSync()
